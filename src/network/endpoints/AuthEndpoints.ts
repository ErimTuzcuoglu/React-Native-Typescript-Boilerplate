import Config from '@config/environment';

/**
 * You can define your endpoints like
 *
 * `${Config.API_URL}/get`,
 *
 */

export default {
  get: `${Config.API_URL}/get`,
  login: `${Config.API_URL}/login`
};
